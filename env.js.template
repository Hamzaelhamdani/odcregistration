/*
 * env.js.template
 * Template pour la génération de env.js
 * Les placeholders seront remplacés par les vraies valeurs
 */

(function() {
    "use strict";

    // Configuration Supabase
    window.ENV = {
        SUPABASE_URL: '__SUPABASE_URL__',
        SUPABASE_ANON_KEY: '__SUPABASE_ANON_KEY__'
    };

    // Log de vérification (sans afficher la clé complète)
    console.log('🔧 Configuration Supabase chargée:', {
        url: window.ENV.SUPABASE_URL,
        hasKey: !!window.ENV.SUPABASE_ANON_KEY
    });
})();
